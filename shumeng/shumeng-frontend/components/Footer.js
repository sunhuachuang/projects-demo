import React, { Component, PropTypes } from 'react';

export default class Footer extends Component {
  render() {
    return (
      <div class="page-header">
        <h1>this is footer <small>Subtext for footer</small></h1>
      </div>
    );
  }
}

Footer.propTypes = {
};
